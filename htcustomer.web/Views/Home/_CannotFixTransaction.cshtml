@model htcustomer.service.ViewModel.Transaction.TransactionHomeViewModel

<div class="list-group-item" style="position: relative" id="transaction-detail-@Model.TransactionID">
    <button class="btn btn-sm btn-danger btn-action btn-deliver-transaction"  data-id="@Model.TransactionID" data-url="@Url.Action("DeliverTransaction","Device",new { transactionId=Model.TransactionID})"><i class="fa fa-send"></i></button>
    <h5 class="list-group-item-heading">
        <label class="label device-title">@Model.Category.Name</label>
        <a href="#" data-placement="bottom" data-toggle="tooltip" title="@Model.Customer.Phone">@Model.Customer.Name</a>
        <hr class="seperator-device" />
    </h5>
    <div class="list-group-item-text">
        <label class="label device-description">Description</label>
        <ul>
            <li>
                <p>@Model.DeviceDescription</p>
            </li>
        </ul>
        <label class="label device-note">Note</label>
        <ul>
            <li>
                @Model.CannotFixNote
            </li>
        </ul>

    </div>
</div>

